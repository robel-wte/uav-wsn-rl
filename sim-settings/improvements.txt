make the following improvements on output plots to make the plots clear and publication ready:
- remove unnecessary artifacts like +- stdev plots, additional labels, ...
- make plot area borders a bit thicker, grid lines also (but lighter than border lines)


now i want to generate a publication ready plots. 1) once again revise and analyze carefully if there are any inconsistencies in all the results and/or plots and codes. go through each code and result, refine it, run final build and simulation, regenerate all outputs including network topology maps and trajectories (at round 1, 100, 1000).



- make the plots start from zero fro all that applies.


detailed analysis of the simulation for: timing, details of parameters/variables made fixed and variable, details of metrics and how and why they vary during the simulation, routing specific impacts/relation to routing, 
analysis should be complete, ready for a high end journal paper (results and analysis section)


ensure effectiveness of clustering:
- plot number of packets generated vs aggregated



a) first commit and push all changes
b) modify the uav-wsn-project-complete.zip file based on the current changes/files
c) optimize the code and simulation as follows:
1) optimize round time: reduce the 150s round time to a minimum possible without affecting any timing,
2) ignore the fixed 1.1s per waypoint timing and make the waypoint timing adaptive based on the T_c formula, again without affecting any other timing,
3) ensure max collection window for all network area (number of waypoints should be enough to cover more chs, as there are temporary chs also)
4) apply all these changes, modify the respective codes, build and run simulation, update all results and plots

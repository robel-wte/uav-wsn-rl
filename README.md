uav-wsn-ql

Clean, history-free import of the uav-wsn-bm project.
This repository was created from /home/wte/uav-wsn-bm with no Git history.

Notes:
- Large build/output directories are ignored by `.gitignore` (out/, results/, plots/).
- If you want the original Git history, work in the original repository instead.

